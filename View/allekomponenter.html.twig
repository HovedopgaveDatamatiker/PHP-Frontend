<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Alle Komponenter</title>
</head>
<style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }
</style>
<body>

<table>

    <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Specifications</th>
        <th>Price</th>
        <th>Bulk</th>
        <th>Link</th>
        <th>Note</th>
        <th>Estimated Delivery</th>
        <th>Available</th>
        <th>Reserved</th>
        <th>Available w. Reserved</th>
        <th>Actions</th>
    </tr>
    {% for c in Components %}
    <tr>
        <td>{{ c.Id }}</td>
        <td>{{ c.Title}}</td>
        <td>{{ c.Specification }}</td>
        <td>{{ c.Price }}</td>
        <td>{{ c.Quantity}}</td>
        <td>{{ c.Link }}</td>
        <td>{{ c.Note}}</td>
        <td>{{ c.EstDelivery }}</td>
        <td>{{ c.Available }}50</td>
        <td>{{ c.Reserved }}</td>
        <td>{{ c.AvailableReserved }}</td>
        <td>
            <form action="http://localhost:8080/PHPProjects/Hovedopgave/Controller/deletekomponent.php" method="POST">
                <input type="hidden" value="{{c.Id}}" name="Id" />
                <input type="submit" value="Delete" name="SletKnap" />
            </form>
            <a href="#popup2">
            <form action="">
                <input type="hidden" value="{{c.Id}}" name="Id" />
                <input type="button" value="Edit" name="EditKnap" />
            </form>
            </a>
        </td>

    </tr>
    {% endfor %}

</table>

</body>
</html>